<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="Home"
        android:icon="@drawable/home_icon"
        app:showAsAction="always"
        android:id="@+id/home_bottom"/>

    <item android:title="search"
        android:icon="@drawable/search_icon"
        app:showAsAction="always"
        android:id="@+id/search_bottom"/>

    <item android:title="My Cars"
        android:icon="@drawable/ic_baseline_directions_car_24"
        app:showAsAction="always"
        android:id="@+id/my_cars_bottom"/>

    <item android:title="Public Chat"
        android:icon="@drawable/question_icon"
        app:showAsAction="always"
        android:id="@+id/message_bottom"/>

    <item android:title="Profile"
        android:icon="@drawable/profile_icon"
        app:showAsAction="always"
        android:id="@+id/profile_bottom"/>
</menu>